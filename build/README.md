# Build resources

Place optional packaging assets here for electron-builder:

- `icon.ico` — Windows application icon (256x256 or multi-size ICO recommended)

If no icon is provided, electron-builder uses its default application icon.
